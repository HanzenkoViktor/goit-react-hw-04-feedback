import styled from '@emotion/styled';

const SectionTag = styled.section`
  margin-left: auto;
  margin-right: auto;
  margin-top: 20px;
`;

const TitleSection = styled.h2`
  font-weight: 600;
  font-size: 25px;
`;

export { SectionTag, TitleSection };
